package com.example.stopgap

import dev.sku20.ir.InstanceRegistry
import io.helidon.webserver.http.HttpRouting

fun initEndpointPaths(
    routes: HttpRouting.Builder,
    registry: InstanceRegistry
) {
    // todo:
}