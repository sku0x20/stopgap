name: setupCi

runs:
  using: "composite"
  steps:
    - name: Setup Java
      uses: actions/setup-java@v5
      with:
        distribution: 'graalvm'
        java-version: 25
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v6
